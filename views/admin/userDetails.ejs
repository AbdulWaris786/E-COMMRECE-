<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Addresses - Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            background: linear-gradient(135deg, #F6D365 0%, #FDA085 100%);
            min-height: 100vh;
            overflow: hidden;
            animation: gradientAnimation 10s ease infinite;
        }

        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        h1 {
            margin-bottom: 20px;
            color: #fff;
        }

        .user-address {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            width: 400px;
            display: flex;
            flex-direction: column;
            background-color: #fff;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 30px;
            background-color: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
        }

        .user-address-item {
            display: flex;
            margin-bottom: 15px;
        }

        .user-address-label {
            font-weight: bold;
            margin-right: 10px;
            width: 120px;
            color: #555;
        }

        .user-address-value {
            color: #333;
        }
    </style>
</head>
<body>
    <h1>User Address</h1>

    <!-- Example user address (replace with actual data) -->
    <%  address.addresses.forEach((item)=>{%>
    <div class="user-address">
        
        <div class="user-address-item">
            <div class="user-address-label">First Name:</div>
            <div class="user-address-value"><%=item.firstName%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">Last Name:</div>
            <div class="user-address-value"><%=item.lastName%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">Phone:</div>
            <div class="user-address-value"><%=item.phone%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">Street Address:</div>
            <div class="user-address-value"><%=item.streetAddress%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">Country:</div>
            <div class="user-address-value"><%=item.country%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">State:</div>
            <div class="user-address-value"><%=item.state%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">City:</div>
            <div class="user-address-value"><%=item.city%></div>
        </div>
        <div class="user-address-item">
            <div class="user-address-label">Pin Code:</div>
            <div class="user-address-value"><%=item.pinCode%></div>
        </div>
    </div>
    <%})%>

</body>
</html>
